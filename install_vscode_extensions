code
if [ $? -eq 0 ]
then
  extensions=(
    castwide.solargraph
    eamodio.gitlens
    zxh404.vscode-proto3
  )

  for extension in ${extensions[@]}
  do
    code --install-extension $extension
  done
fi
